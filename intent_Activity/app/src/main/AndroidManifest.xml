<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.intent_activity">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Intent_Activity">
        <activity android:name=".PhoneBookActivity"></activity>
        <activity android:name=".PhoneBookDetailActivity" />
        <activity android:name=".PhoneBookRecylcerActivity" />
        <activity android:name=".PhoneBookRecyclerView" />
        <activity android:name=".RecyclerView" />
        <activity android:name=".RecycleView" />
        <activity android:name=".AddViewActivity" />
        <activity android:name=".ListViewActivity1" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".AddViewActivity" />
        <activity android:name=".Library" />
        <activity android:name=".Context" />
        <activity android:name=".NullSafety" />
        <activity android:name=".FragmentActivity" />
        <activity android:name=".Intent2" />
        <activity android:name=".Intent1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>